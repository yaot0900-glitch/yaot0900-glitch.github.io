# 2026-06-14 — 游戏流程重构

## ✅ 完成事项

### 三赛道必玩重构
- 重写 `GameContext.tsx`：移除前后测状态（preTestTrack, postTestTrack, postTestAnswers 等），新增 trackOrder, currentTrackIndex, trackQuestionIds
- 重写 `useGame.ts`：移除后测相关函数（startPostTest, submitPostTestAnswer 等），新增 initGame, submitTrackAnswer, completeCurrentTrack, getCurrentTrackQuestionIds, getTrackScore, getTrackStats
- 删除 `TrackSelectPage.tsx`（不再需要赛道选择）
- 修改 `HomePage.tsx`：取名后调用 initGame()，跳转到 /pre-test-guide
- 重写 `QuizPage.tsx`：移除前后测模式，新增赛道信息显示 + 赛道完成总结弹窗
- 重写 `RewardPage.tsx`：三赛道成绩总览（每赛道得分、正确率）
- 重写 `ReportPage.tsx`：删除后测对比区块，新增三赛道成绩对比卡片

### 问卷星集成
- 新建 `PreTestGuidePage.tsx`：前测问卷引导页（链接 + 二维码 + "已完成"按钮）
- 新建 `PostTestGuidePage.tsx`：后测问卷引导页（同上结构）
- 新建 `src/config/survey.ts`：问卷链接、二维码路径、Google Script URL 配置

### Google Sheets 数据收集
- 新建 `src/utils/dataCollector.ts`：构建 GameDataPayload + 静默提交到 Google Sheets
- ReportPage 加载时自动提交数据
- 新建 `docs/google-sheets-setup.md`：详细设置指南（含 Apps Script 代码）

### 路由更新
- 移除 `/select`，新增 `/pre-test-guide` 和 `/post-test-guide`

## 🚧 待办事项
- [ ] 用户配置问卷星链接和 Google Sheets URL（编辑 `src/config/survey.ts`）
- [ ] 按 `docs/google-sheets-setup.md` 指南部署 Google Apps Script
- [ ] 放置问卷二维码到 `public/images/`
- [ ] 重新 `npm run build` 部署
- [ ] 推送至 GitHub

## 🐛 遇到的问题
（无）
